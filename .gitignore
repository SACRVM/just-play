# ── .NET / build output ─────────────────────────────────────────────────────
bin/
obj/
publish/
*.user
*.suo
*.userprefs
project.lock.json
project.fragment.lock.json
artifacts/

# ── IDE / tool state ────────────────────────────────────────────────────────
.vs/
.idea/
.vscode/
*.swp

# ── Local agent / memory ────────────────────────────────────────────────────
# Claude Code's per-project memory (personal notes, not project artefacts).
.claude/
# Other co-pilot scratch dirs.
.firepit/

# ── Avalonia ────────────────────────────────────────────────────────────────
*.axaml.cs.bak

# ── Runtime debris ──────────────────────────────────────────────────────────
*.log
*.stackdump
*.pdb
crashlogs/

# ── OS ──────────────────────────────────────────────────────────────────────
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
$RECYCLE.BIN/

# ── Session-local notes ─────────────────────────────────────────────────────
# Dated session hand-off documents are deliberately not committed — they are
# point-in-time aids, not project history.
HANDOVER-*.md
